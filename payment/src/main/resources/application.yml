server:
  port: 8084  # Payment service port

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/payment_db
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

razorpay:
  key: rzp_test_SYJLFbJPFryV4y       # test key
  secret: kGRlvpuOBLOMKLZBcxxnPGgu # test secret

kafka:
  bootstrap-servers: localhost:9092
  topic:
    payment: payment-topic

